# format: path<TAB>expectation<TAB>note
# parser command: lake exe nixparserlean --format json --file
e2e/corpus/smoke/eval-arithmetic.nix	pass	JSON output succeeds for surface, core, and eval parser modes
