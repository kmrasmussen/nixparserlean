# Recursion

`example.nix` demonstrates terminating recursive `let` bindings and recursive
attribute sets. It avoids self-recursive cycles, which are covered separately
as expected eval failures.

It is referenced by `e2e/eval-manifest.txt`.
