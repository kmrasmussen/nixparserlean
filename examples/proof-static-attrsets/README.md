# Proof Static Attrsets

`example.nix` is a small static dotted-attrset fragment. It is intended as a
readable desugaring/proof fixture rather than a broad language showcase.

It is referenced by `e2e/desugar-manifest.txt`.
