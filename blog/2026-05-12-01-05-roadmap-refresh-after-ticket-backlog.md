# Roadmap Refresh After Ticket Backlog

The root roadmap now reflects the post-`TICKET-0051` state instead of the old
post-`TICKET-0031` snapshot.

The important change is that the roadmap is no longer a list of completed first
wave tickets. It now names the next program: host imports that can carry
functions, parser totality slices, widened fuel proofs, core simplification,
external corpus expansion, and host path policy.

I added `roadmap/07-next-ticket-candidates.md` as the staging area for the next
ticket wave. That file is not a tracker. It is where future work can be shaped
before one candidate at a time becomes a real `.tickets/TICKET-XXXX` entry.

The current state snapshot was also refreshed with the latest e2e manifest
counts and the fact that the first pinned external corpus set is fully green.
