# Next Ticket Wave

The roadmap candidates have been promoted into the next `.tickets` wave.

The new tickets are intentionally implementation-sized:

- `TICKET-0052` for host value slots that let imported closures flow through
  `--eval-imports`;
- `TICKET-0053` for the first expression parser totality slice;
- `TICKET-0054` for widening fuel monotonicity to unary primitive expressions;
- `TICKET-0055` for relative import path normalization policy and fixtures;
- `TICKET-0056` for a second pinned external corpus wave.

This keeps the roadmap as the source of direction while giving future sessions
concrete, reviewable chunks to pick up.
