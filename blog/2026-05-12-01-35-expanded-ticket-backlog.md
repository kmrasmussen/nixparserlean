# Expanded Ticket Backlog

The backlog is now large enough for parallel work.

I promoted the second roadmap wave and several larger-plan slices into tickets
`TICKET-0057` through `TICKET-0076`. The queue now covers parser totality,
proof widening, host effects, external corpus infrastructure, JSON diagnostics,
core-language decisions, and backlog maintenance.

The important constraint remains the same: each ticket is sized for one
reviewable contribution with its own acceptance gate. The queue is bigger, but
the units are still meant to be small enough to implement, verify, blog, and
commit independently.
